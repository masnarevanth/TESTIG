installing nginx webserver on linux2023
---------------
#!/bin/bash
amazon-linux-extras install epel -y
sudo yum update
sudo yum clean all
sudo yum install nginx1.12 -y
sudo systemctl enable nginx -y
echo "<div><h1>$(cat /etc/ip)</h1></div>" >> /usr/share/nginx/html/index.html


installing httpd webserver 
-----------
#!/bin/bash
yum update -y
sudo su
yum install httpd -y
systemctl start httpd —now
echo "<h1>Hello World from $(hostname -f)</h1>" > /var/www/html/index.html
